import { Typography } from '@mui/material';

const PaymentHistory = () => {
    return (
        <Typography>
            Payment History
        </Typography>
    );
};

export default PaymentHistory;